name: music_ab_test_dbt
version: '1.0.0'
config-version: 2

# 2. Profile setting
profile: 'jaffle_shop'

# 3. Path setting
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# 4. Model setting
models:
  music_ab_test_dbt:
    +enabled: true
    staging:
      +materialized: view
    marts:
      +materialized: table